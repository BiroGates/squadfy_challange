meta {
  name: List Knowledge - Techcorp
  type: http
  seq: 1
}

get {
  url: {{base_url}}/organizations/{{techcorp_org_id}}/knowledge
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # List Knowledge
  Retorna todos os knowledgements da organização (apenas metadados, sem conteúdo do S3).
  Required role: ORGANIZATION ou ADMIN
}
